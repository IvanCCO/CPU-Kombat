// página inicial (index)


// Funçao para fechar a pagina
function close_tab() {
    if (confirm("Deseja desistir Aluno SPTECH?")) {
      window.close();
    }
  }

  // Começo da configuração do jogo 


  // Setando a variavel como 1 para começar como fácil por default 


  var modeVar = 1;

  console.log(modeVar)



  // Modo normal 
  if(modeVar == 1){

  }

  // Modo Normal
  else if (modeVar == 2){

  // Modo dificil
  }else if (modeVar == 3){

    //CPU-master
  }else{

  }

